/gsd:quick Implement a calculator. Create `calculator.py` with a `calc(expression: str) -> float` function that parses and evaluates math expressions. Support +, -, *, / with correct operator precedence, parentheses, unary negation, decimal numbers, whitespace handling. Raise ZeroDivisionError for division by zero, ValueError for invalid expressions. Do NOT use eval/exec/compile. Add CLI interface that reads expression from command-line args, prints result to stdout, exits 1 on error with message to stderr. Single file, no external deps.
